filebeat:
  prospectors:
    -
      paths:

      - /var/log/messages
      input_type: log
      document_type: docker_syslog
output:
  logstash:
    hosts: ["logstash1:5000", "logstash2:5000"]
    loadbalance: true
shipper:
logging:
  files:
    rotateeverybytes: 10485760 # = 10MB

